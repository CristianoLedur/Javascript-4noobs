console.log("2.0 Dom - Introdução ao DOM (Document Object Model)");

// DOM -> Sempre que uma página é carregada, o navegador cria um DOM (Document Object Model) para a gente.

/*
O DOM define:

1-Os elementos como objetos,
2-As propriedades dos elementos,
3-Os métodos,
4-Os eventos.

Basicamente, o DOM define um maneira de pegar(get), mudar(change), adicionar(set) ou deletar(delete) cada elemento HTML no DOM.
*/